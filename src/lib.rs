// all memory allocation must be explicitly injected
#![no_std]

pub fn run(mem: &mut [u8]) {
    // todo: start up and initialisation
    // must pass in an interface to model processes
    // must pass in an interface to model runtime features
}
